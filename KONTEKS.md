# KONTEKS.md — Konteks Aktif & Status Sesi

> **FILE INI DIBACA PERTAMA** setiap sesi baru atau sesi hilang — sebelum RENCANA.md.
> Tujuan: supaya AI di sesi baru langsung tahu topik apa yang sedang dibahas dan tidak ada pembahasan yang terlewat.
> **WAJIB diupdate setiap prompt**: bagian "Konteks Aktif" + log di bawah.

---

## 1. Konteks Aktif (UPDATE TIAP PROMPT)

- **Topik sedang dibahas**: **Setup-v2 kanonis** — `dist/EvernightLanguage-0.1.0-Setup-v2.exe` (payload penuh: kill+update+CLI baru). Reinstall `--diam` dari v2 terverifikasi, `update`/`system info` jalan di PS baru. Fase 7 ditambah **7F — Paket Rilis Per-Versi** (skrip `paket-rilis.ps1`, paket per-versi isi lengkap + ZIP + SHA256).
- **Keputusan terbaru (2026-09-18)**: **LSP ditunda & crates.io ditunda** oleh user; prioritas = revisi ikon dulu, lalu desain bagian uninstaller (user mau buat mockup di Google Stitch) sebelum Fase 7 lainnya.
- **Keputusan terbaru (2026-09-16)**: **Installer memakai egui/eframe (Rust), BUKAN Inno Setup** — Inno Setup berbasis Delphi VCL, tidak bisa animasi halus. Crate `installer/` berada **di luar workspace** agar `+crt-static` tidak bentrok dengan `winit`/`eframe`. Palet installer dari gambar referensi user (plum/rose): latar `#37313D`, well `#2F2430`, kartu `#443C4A`, teks `#F8DCE8`, aksen `#D3A0B9`. Font Plus Jakarta Sans + JetBrains Mono (OFL). Panel maskot **28% lebar penuh atas-bawah**. `installer/src/tema.rs` = satu-satunya berkas visual yang perlu disentuh user.
- **Sedang dikerjakan**: **6H ✅ + 6I ✅ LIVE**. Menunggu arahan user (Fase 7 masih ditunda).
- **Langkah berikutnya**: (1) Rilis versi baru bila siap (naikkan `version/version` + taruh file di folder `version/` + push → user `update evernight system`); (2) Fase 7 bila user cabut penundaan.
- **Perintah CLI tersedia**: `evernight <berkas>`, `evernight run <berkas>`, `evernight format <berkas> [--cek|--keluar]`, `evernight lint <berkas>`, `evernight pkg init|jalankan|daftar`, `evernight <berkas> --debug|--waktu|--bytecode|--ast|--tokens|--cek`.
- **Jangan lupa**: Zero dependencies untuk `evernight_core`/`_vm`/`_cli` (installer adalah **pengecualian sadar**). Semua error message wajib berformat Bahasa Indonesia (`BAHAYA [KODE]` / `PERINGATAN [KODE]`).
- **Catatan toolchain**: default = `stable-x86_64-pc-windows-gnu` (MSVC **tidak punya linker** di mesin ini). Konfigurasi build statik kini **target-spesifik** di `.cargo/config.toml` (`[target.x86_64-pc-windows-gnu]`, bukan `[build]`) agar aman di CI.
- **Catatan build installer**: butuh `dlltool.exe` — tersedia di `C:\msys64\ucrt64\bin`, dan `build-installer.ps1` menambahkannya ke PATH otomatis. Antivirus (McAfee) sempat memblokir build script crate baru (os error 225); build dari folder proyek (bukan temp) menghindari ini. Target dir installer saat dev: gunakan folder temp (`CARGO_TARGET_DIR`) bila `target/` terkunci.
- **Catatan installer 6F**: `installer/src/tema.rs` memuat semua warna/font/ukuran window/animasi. Uji tiap halaman tanpa mengklik: `evernight-installer --mulai N` (N = 0..5). Pratinjau di `installer/aset/tinjau/`.
- **Utang teknis**: code signing installer (SmartScreen), master SVG logo, uji installer di komputer bersih.
- **Struktur editors/vscode saat ini**: `syntaxes/evernight.tmLanguage.json`, `themes/nusantara-dark.json`, `themes/nusantara-light.json`, `snippets/eve.json`, `icons/evernight-icon-theme.json` (98.686 B, 349 iconPath: 348 Symbols + `_eve`), `icons/evernight-file.png` (640 B), `icons/evernight-icon.png` (6.259 B), `icons/icons/files/` (249 SVG), `icons/icons/folders/` (104 SVG), `src/extension.ts` (autocomplete ~60 item), `out/extension.js` (build tsc bersih). `build-vsix.ps1` — patch vsix dasar (semua berkas ikon rekursif + package.json label IconStyles).

## 2. Topik yang Sudah Dibahas (jangan sampai terlewat)

- [x] Keputusan dasar bahasa (nama, ekstensi `.eve`, paradigma, bytecode+VM, ARC) — lihat `KONSEP.md`
- [x] `ARRAY.md` — semantik daftar final (mirip Python)
- [x] `TIPE.md` — sistem tipe data final (angka tunggal, teks, bolean, kosong, daftar, kamus, fungsi, objek)
- [x] `ERROR.md` — sistem error terstandarisasi (`BAHAYA [KODE]` / `PERINGATAN [KODE]`, NaN fatal)
- [x] `STDLIB.md` — rancangan lengkap 7 kategori modul pustaka standar + ~35 fungsi utilitas
- [x] `FUNCTION.md` — spesifikasi lengkap jenis fungsi, closure, aturan `fungsi`, loop, dan error flow
- [x] `KEYWORD.md` — finalisasi lengkap ~50 kata kunci Fase 1 (variabel, fungsi, kembali, impor, dll)
- [x] `LIFECYCLE.md` — lifecycle runtime final (8 tahap, hybrid REPL+File, lazy-load impor)
- [x] Sistem konteks-sesi (`KONTEKS.md` ini) — selesai dibuat

## 3. Keputusan Halus / Detail Kecil yang Mudah Hilang

(Catatan keputusan kecil yang tidak cukup penting masuk `RENCANA.md` tapi perlu diingat.)

- Array: `tambah`/`sisip`/`hapus`/`ambil`, error out-of-bound pakai `BAHAYA`.
- String: `besar()`/`kecil()`, `bersih()`, `potong()`, properti `.panjang`.
- Tipe: strict type checking (tanpa implicit coercion `10 + "5"` -> error `BAHAYA`).
- Error: format `LEVEL [KODE]: Baris X - Deskripsi!`, fungsi/kelas kosong -> `PERINGATAN [WKFUNG]`/`[WKCLASS]`, NaN -> `BAHAYA [NaN]`.
- Alur Fungsi: wajib return eksplisit `kembali`, parameter default diizinkan, loop `selama`/`untuk`/`berhenti`/`lanjut`, error block `coba`/`tangkap`/`lempar`/`pastikan`.
- Keyword final: `variabel` (bukan `nyatakan`), `fungsi` (bukan `fn`), `impor` (bukan `pakai`), `kembali` tetap `kembali`.

## 4. Log Konteks (append ke atas, terbaru dulu)

- **2026-09-20 (v2)**: **Setup-v2 jadi artefak kanonis.** User reinstall pakai v2 lama (payload kill saja) sehingga `update.cmd` hilang dari `bin/` → `update` tak dikenal di PS (bukan masalah alias; PS lama juga PATH basi). Perbaikan: rebuild payload penuh ke `dist/EvernightLanguage-0.1.0-Setup-v2.exe` (SHA `a91ef6de...`, sama dgn v1 karena payload identik), SHA256SUMS dua baris, reinstall `--diam` dari v2 → bin/ lengkap (exe+kill+update). Verifikasi PS baru: `update evernight system` → zhongk ✅, `evernight system info` ✅. Catatan: file `Setup.exe` lama di dist dibiarkan apa adanya.

- **2026-09-20 (revisi)**: **Catatan versi + `evernight system info` + Setup 0.1.0 revisi.** `version/catatan-0.1.0.txt` (ASCII saja — em-dash bikin mojibake terminal). Subcommand `evernight system <aksi>` (`info`: versi+lokasi+ukuran dari registri, isi versi live GitHub + fallback offline; tanpa aksi → BAHAYA [ARG]). 2 test CLI baru (28/28 hijau). `update.ps1` cetak catatan perubahan sebelum memasang (teruji mock). Rantai build penuh: CLI→payload→installer→dist Setup.exe 9.59 MB SHA `a91ef6de...`. Reinstall `--diam` terverifikasi: bin/ berisi exe baru + kill + update, `system info` + `update evernight system` (zhongk) jalan dari instalasi. `version/version` tetap 0.1.0 (bukan update). Push `81249db`.

- **2026-09-20 (lanjut)**: **Push GitHub + updater LIVE.** Push 2 commit ke `Evanight234/EvernightLang` (main). `pembaruan/version` disatukan jadi `version/version` (satu kanonis, sesuai path yang dibaca updater). `$REPO` diisi alamat asli. Uji live: `update evernight system` → fetch GitHub asli → `Istriku lagi sibuk jangan dingagu` ✅. Setup.exe rebuild 9.57 MB SHA `135d6723...`, `uninstall.exe` terpasang diupdate. Logika tanpa-update sesuai maumu: versi lokal ≥ versi GitHub (termasuk folder hanya berisi v1) → pesan zhongk; versi GitHub lebih baru → langsung pasang update. Berkas `version` kosong/rusak → BAHAYA (tanda manifes salah isi, bukan "tidak ada update").

- **2026-09-20**: **6I SELESAI — Sistem Updater CLI.** `update.cmd` (validasi argumen) + `update.ps1` (cek versi via `version/version` GitHub raw → banding `[System.Version]` → unduh exe+vsix dari folder `version/` → timpa → reinstall ekstensi best-effort → catat DisplayVersion). Tanpa update: `Istriku lagi sibuk jangan dingagu`; ada update: `terupdate ke versi <versi>, jangan panggil istriku lagi dasar karbit` (verbatim user). Exe terkunci → penimpaan terjadwal. Flag `--cek` dry-run. Di-embed ke payload installer → `bin/` otomatis. `pembaruan/version` lokal (0.1.0). Setup.exe rebuild 9.57 MB SHA `5b592d49...`. Teruji: sintaks OK + 5 skenario (argumen/BAHAYA jaring/zhongk/cabang update/`--cek`) + PATH resolve dari C:\ + 21 test installer hijau. Prasyarat: push repo + ganti `<akun>/<repo>` (uji update nyata menyusul).

- **2026-09-19**: **6H SELESAI — Sistem Uninstaller 3 Tahap + kill.cmd/kill.ps1 + PowerShell Profile.** Wizard pencopotan: **Konfirmasi** → **Menghapus** → **Selesai**. `kill evernight system` jalan di CMD (`kill.cmd` di bin/) DAN PowerShell (`function kill` di `$PROFILE` dengan `Remove-Item alias:kill` untuk override alias bawaan `Stop-Process`). `kill.ps1` di-embed di payload installer, dicopy ke `bin/` otomatis. Installer registrasi function ke `$PROFILE` saat install, hapus saat uninstall. Build: `Setup-v2.exe` 9.56 MB, SHA `63a46011...`. 21 test hijau, clippy 0 warning. Folder terpasang: `bin/evernight.exe` + `bin/kill.cmd` + `bin/kill.ps1`, `assets/`, `docs/`, `uninstall.exe`.

- **2026-09-16**: **FASE 6 SELESAI SELURUHNYA (6D-3, 6D-4, 6D-6, 6E, 6F).**
  - **6D-3 Formatter** (`crates/evernight_fmt`): berbasis token (bukan AST) supaya komentar tidak hilang; idempoten; `evernight format <berkas>` + `--cek` + `--keluar`. 14 test.
  - **6D-4 Linter** (`crates/evernight_lint`): 7 aturan baru (WKHURUF/WKIMPOR/WKPANJANG/WKPARAM/WKSARANG/WKMATI/WKMAGIS) + warning compiler; `evernight lint`. 11 test.
  - **6D-6 Package manager**: `evernight pkg init|jalankan|daftar`, parser JSON zero-dep (`json.rs`), manifest `eve.json`. 7 test.
  - **6E CI & rilis**: `git init` + `.gitignore`/`.gitattributes`; `.github/workflows/ci.yml` + `rilis.yml`; `.cargo/config.toml` diubah ke target-spesifik agar `+crt-static` tidak merusak CI; 12 contoh + 3 golden dirapikan.
  - **6F Installer GUI egui** (`installer/`, di luar workspace): panel maskot 28%, palet dari referensi user, font Jakarta Sans + JetBrains Mono, bilah judul kustom, 6 halaman, animasi mikro, per-user/per-machine, `uninstall.exe`. Hasil `EvernightLanguage-0.1.0-Setup.exe` 8.35 MB + SHA256. Pratinjau 6 halaman di `installer/aset/tinjau/`; flag `--mulai N`.
  - **Verifikasi nyata**: install → PATH 32, `.eve` aktif, Apps & Features terdaftar, semua subcommand CLI jalan; uninstall → folder terhapus penuh, PATH 31, registry bersih.
  - **5 bug diperbaiki**: BOM UTF-8; `UninstallString` salah target; kunci berkas saat uninstall (dijalankan dari temp); bantuan global menelan subcommand; paket berisi biner basi (penjaga di `build-installer.ps1`).
  - Seluruh test hijau, `cargo fmt --check` bersih, clippy 0 warning. Fase berikutnya: **Fase 7**.
- **2026-09-14**: **FASE 6D-1 & 6D-2 SELESAI — Mode Debug & Profiler Mini.** `--debug` mencetak trace instruksi bytecode ke stderr (`[offset] baris N | NamaOpcode`) via `Vm::debug_trace` + `OpCode::name()`; `--waktu` mencetak waktu eksekusi, total instruksi, dan tabel frekuensi opcode via `Vm::profile_mode` + `Vm::profile_summary()` (counter `[usize; 256]`, zero-dep). Flag keduanya selalu menjalankan program. 2 test baru di `cli_tests.rs`; seluruh workspace hijau, clippy 0 warning. Sampingan: ditambahkan `tests/golden/01_halo.harapan` (file `.eve` tanpa pasangan `.harapan` membuat golden test gagal). Lanjut **6D-3 Formatter**.
- **2026-09-14**: **FASE 6C SELESAI — Integrasi Otomatis Ekstensi saat Install.** `install.ps1` kini punya fungsi `Cari-Editor` (deteksi 6 editor keluarga VS Code via CLI/PATH + lokasi standar; **tidak** lewat folder data). Alur **konfirmasi interaktif**: `Pasang ekstensi ...? (y/t)` → `<editor-cli> --install-extension <vsix> --force`. Opsi baru `-TanpaEkstensi` & `-TanpaKonfirmasi`. `uninstall.ps1` menawarkan pencopotan ekstensi (`y/t`) + `-SimpanEkstensi`/`-TanpaKonfirmasi`. `docs/PANDUAN.txt` & `BACA-AKU.txt` diperbarui. **Bug diperbaiki**: peringatan stderr CLI (`antigravityAnalytics`) sempat dianggap galat fatal. Verifikasi nyata: deteksi Antigravity, prompt jalan, `-TanpaEkstensi` lewat, uninstall `-SimpanEkstensi` pertahankan, PATH 32↔31 bersih, ZIP dibangun ulang (~694 KB). Lanjut **6F**.
- **2026-09-14**: **RENCANA INSTALLER & DESAIN DITAMBAHKAN.** Ditetapkan alur distribusi end-user memakai **satu `Setup.exe`** (Inno Setup 6): mode hak akses `PrivilegesRequiredOverridesAllowed=dialog` (pengguna pilih "Hanya saya" tanpa UAC / "Semua pengguna" dengan UAC), wizard Bahasa Indonesia, opsi PATH/asosiasi `.eve`/ekstensi editor/pintasan, uninstaller di *Apps & Features*. **Fungsional = 6F**, **desain visual final + uji komputer lain = 7E** (akhir Fase 7). **Code signing ditunda** (SmartScreen, utang). **6C ditunda** (diadaptasi ke 6F). Dokumen `RENCANA.md`, `PROGRES.md`, `KONTEKS.md`, `CHANGELOG.md`, `AGENTS.md` diperbarui.
- **2026-09-14**: **FASE 6B SELESAI — Distribusi Compiler Mandiri.** Biner release `evernight.exe` **1.74 MB statis** (`+crt-static`; tanpa DLL MinGW, hanya `msvcrt.dll`). Dibuat folder **`paket/evernight-0.1.0-windows-x64/`**: `bin/`, `assets/`, `docs/PANDUAN.txt`, `extensions/evernight-language-0.1.0.vsix`, `install.ps1`+`install.cmd`, `uninstall.ps1`+`uninstall.cmd`, `BACA-AKU.txt`; ZIP `paket/evernight-0.1.0-windows-x64.zip` (691 KB). Installer per-user **tanpa admin**: salin ke `%LocalAppData%\Programs\Evernight\`, PATH user idempoten, asosiasi `.eve` (`HKCU\Software\Classes`, ProgID `EvernightFile`, label **"Evernight files"**, `evernight.exe run "%1"`), `SHChangeNotify`, flag `-Uji`/`-TanpaAsosiasi`. Uninstaller `.ps1`/`.cmd` (bukan `.exe`) + penghapusan tertunda bila terkunci. **Verifikasi nyata**: PATH 31→32, `evernight --versi` OK, `run` OK, **klik-ganda `.eve` berjalan**, install 2× idempoten, uninstall kembali bersih (31). Lanjut **6C**.
- **2026-09-14**: **FASE 6A SELESAI — Ekstensi `.vsix` terpasang di Antigravity IDE.** Keputusan user: lisensi **MIT** (hak: Satriyo), publisher **Satriyo**, uji di **Antigravity IDE**, eksekusi Fase 6 urut sub-fase 6A→6E. Dibuat: `LICENSE` MIT (root + ekstensi), `package.json` lengkap (publisher/license/icon/repository/keywords), `.vscodeignore`, ikon ekstensi. Build `evernight-language-0.1.0.vsix` (~20KB, tanpa node_modules/src/test) → terpasang via `antigravity-ide.cmd --install-extension` → **terverifikasi** `satriyo.evernight-language-0.1.0`. **Syntax highlighting `.eve` kini aktif.** README ekstensi diperbarui (Antigravity + aktivasi tema). Lanjut **6B**.
- **2026-09-12**: **FASE 5 SELESAI.** (1) `vm_tests.rs` monolitik dipecah jadi 9 berkas `tests/test_*.rs` + `common/mod.rs`; (2) `Vm` dapat output capture opt-in (`set_capture_output`/`take_captured_output`); (3) `tests/golden/` 6 kasus + `golden_tests.rs` (library API, tanpa spawn exe); (4) `error_codes_test.rs` 12 test seluruh kode error/warning; (5) `fuzz_parser.rs` 15.000 iterasi — **menemukan 2 bug**: lexer index OOB saat escape `\` di EOF & parser stack overflow (diperbaiki dengan `MAX_RECURSION_DEPTH = 64`); (6) `benches/benchmark.rs` zero-dep (kompilasi ~31.8µs, faktorial(20) ~2.0µs, loop 10k ~1.96ms); (7) `TESTING.md`, (8) `CHANGELOG.md` `[0.1.0]`. Bug lain diperbaiki: scope lokal fungsi vs global (`is_function`), `format()` variadik `{0}`/`{1}`. Seluruh test HIJAU, clippy 0 warning. Lanjut **Fase 6**.
- **2026-09-12**: **STRUKTUR RENCANA MENJADI 8 FASE.** Rencana dipisahkan: Fase 5–7 fokus murni pada pengembangan teknis compiler, VM, tooling, performa, dan distribusi sistem (installer mandiri tanpa dependensi Rust/Cargo bagi pengguna akhir). Semua item non-pengembangan (website resmi WASM, dokumentasi publik, materi ajar, promosi, dan komunitas) dipindahkan ke Fase 8 baru ("Dokumentasi Publik, Website & Komunitas"). `RENCANA.md`, `PROGRES.md`, `AGENTS.md`, dan `KONTEKS.md` telah disinkronkan.
- **2026-09-12**: **FASE 4 SELESAI TOTAL.** Subcommand `evernight run berkas.eve` ditambahkan ke CLI runner; snapshot test highlighting (`test/snapshots/highlight.eve` + `.scope`) dibuat; `editors/vscode/README.md` panduan instalasi lokal diselesaikan; spesifikasi distribusi & bundling compiler tanpa dependensi Rust/Cargo bagi pengguna akhir ditulis di `RENCANA.md` Fase 6. Seluruh 70 test workspace HIJAU, clippy 0 warning.
- **2026-09-12**: **FASE 4B SELESAI.** TextMate grammar `editors/vscode/syntaxes/evernight.tmLanguage.json` (dari `token.rs`: keyword kontrol/deklarasi/operator, string, angka, konstanta, fungsi deklarasi+panggil, komentar baris+blok, punctuation). Tema gelap `nusantara-dark.json` (latar `#0b1020`, toska `#0f9d8f`, batik `#f2a65a`) + terang `nusantara-light.json` (krem `#f5f0e8`, `#0b7a70`). Snippet 23 item `snippets/eve.json`. Autocomplete statis ~60 item di `extension.ts` (keyword+builtin+modul stdlib, trigger karakter `." "`). Icon theme `.eve` `icons/evernight-icon-theme.json`. `package.json` ditambah `iconThemes`. Build `tsc` bersih 0 error. 8 core + 52 vm = **60/60 test hijau**. `TEMA.md` palet final diisi. `PROGRES.md` Fase 4 diupdate.

- **2026-09-10**: **FASE 4A SELESAI.** CLI runner default-run `evernight file.eve`, `--run`, `--cek` (syntax & compile check), `--tokens`, `--ast`, `--bytecode` (disassembler VM), `--versi`/`-v`, `--bantuan`/`-h`, `--tanpa-warna` (`NO_COLOR`), argumen program pemisah `--` + `argumen()` (id 75 di VM), error terminal berwarna + cuplikan kode line/caret `^` (`printer.rs`), REPL interaktif (`repl.rs`: prompt `eve>`, multi-baris `{/([`, `:bantuan/:keluar/:muat/:bersihkan`, `run_incremental`). 68/68 test hijau (8 core + 52 vm + 8 cli), clippy 0 warning. `STDLIB.md`, `ERROR.md`, `RENCANA.md`, `PROGRES.md`, `KONTEKS.md` disinkron.
- **2026-09-10**: **FASE 4A DIMULAI.** Instruksi user: kerjakan 4A dan perbarui file MD setiap ada kemajuan. Keputusan: zero-dep REPL, daftar kanonik + drift-test grammar, urutan 4A dulu baru 4B.
- **2026-09-10**: **FASE 3 SELESAI.** `BRAND.md` baru (brand kit final): slogan user *"Evernight bukan hanya karakter game tapi bisa menjadi pelajaran bahwa menyukai karakter juga bisa menjadi motivasi"*, font sans-serif, lisensi bebas pakai dengan atribusi, palet resmi + aturan pakai + peta aset. `LOGO.md` §7–§9 → pointer `BRAND.md` (anti-duplikasi), item 7 `[x]`. RENCANA/PROGRES/AGENTS: brand kit `[x]`, README `[x]`. Utang: master SVG. Lanjut **Fase 4**: REPL + CLI (`evernight coba.eve` default-run, `--`, `argumen()`), tema "Nusantara" + ekstensi VS Code.
- **2026-09-10**: **Fase 3 logo FINAL + keputusan liputan editor.** User setujui logo (review OK). `[~]` difinalisasi: desain `[x]`, icon `.eve` `[x]`; brand kit draft di `LOGO.md` §9; `README.md` placeholder dibuat (item "Pasang logo ke README"). **Keputusan editor (user): keluarga VS Code saja** — satu `.vsix` (VS Code/Cursor/Windsurf/VSCodium, Antigravity dicek saat eksekusi), Marketplace 1 upload, installer integrasi **opt-out**, non-goal 1.0 Sublime/Zed/Neovim/JetBrains. Di-catat di `RENCANA.md` (Fase 4/6 + Log Keputusan) + `PROGRES.md`. Tanpa perubahan kode (58/58 tetap hijau). Lanjut: **Fase 4** atau sisa kecil Fase 3 (brand kit final).
- **2026-09-10**: **LOGO DIPROSES — `assets/logo/` siap.** `logo1.png` user (E serif di badge krem, inspirasi JS) → `logo.png` (master transparan E tipis), `logo_monokrom.png` (E putih), `logo_badge.png` (E ditebalkan ≈39% utk 16px), `icon_16/32/48/256.png` + `icon.ico`, `preview.png`. Klik-test: 16px ASCII terbaca "E". Utang: SVG vektor. `LOGO.md`/`RENCANA.md`/`PROGRES.md`/`KONTEKS.md` disinkron.
- **2026-09-10**: **Fase 3 sisa admin selesai — error code sinkron + unit test lengkap.** `ERROR.md`: 2 baris `TYPE` salah kode dipindah ke `FUNGSI` (bagian baru §B.5, 3 kasus); §G ditambah `RUNTIME`/`MATH`/`JUMLAH`/`ASSERT` (pesan dari string aktual kode); `STACK`/`MEMORY` ditandai *(direncanakan — belum di-raise)*; §4 status finalisasi +2 baris. `vm_tests.rs`: test baru `konsol_cetak_bersihkan_nan` (cetak→Kosong, bersihkan→Kosong, baca_angka("abc")→[NaN]). **58/58 hijau**, clippy 0. `RENCANA.md`/`PROGRES.md`/`AGENTS.md`/`KONTEKS.md` disinkron. **Fase 3 stdlib selese — tinggal Identitas Visual (logo user).**
- **2026-09-10**: **Rencana Fase 6 diperluas — Static CompletionProvider (VS Code).** Fitur autocomplete statis (~60 item: keyword + fungsi bawaan + modul) ditambahkan ke `RENCANA.md` Fase 6. Mekanisme: `vscode.languages.registerCompletionItemProvider` di `extension.ts`, prefix match otomatis saat user ketik, tanpa LSP, zero dependency ke compiler/VM. `PROGRES.md` + `KONTEKS.md` disinkron.
- **2026-09-10**: **Fase 3 item 7 selesai — `impor` lintas berkas v1.** Opcode `ImporModul` (0x95); compiler: modul standar no-op, selain itu `Konstanta(path)` + `ImporModul` + `SimpanGlobalNama(alias/nama-dasar)`. VM: `entry_dir` + `import_cache` + `importing`; `CallFrame` bawa `module_path` + `module_before`; `Henti` pop frame + kumpulkan global baru → `Kamus` ekspor + cache; `Vm::with_dir(dir)`. CLI: meneruskan direktori berkas utama. Siklus impor → `[FILE]`, berkas hilang → `[FILE]`. Sintaks `impor "path" [sebagai alias]`; `dari` ditunda. 7 test baru → **57/57 hijau**, clippy 0. Contoh `examples/import/main.eve` + `helper.eve` diverifikasi via CLI. `STDLIB.md` + `GRAMMAR.md` + `ERROR.md` + `RENCANA.md` + `PROGRES.md` disinkron.
- **2026-09-10**: **Fase 3 item 6 selesai — Modul UTILITAS** (13 builtin id 62–74, stdlib 7/7 LENGKAP). `adalah_*`, `ke_boolean`/`e_boolean`/`bolean` (alias `is_truthy`), `ke_larik`/`daftar` (teks→karakter, kamus→kunci urut alfabetis), `salin` **deep copy** (siklik aman via visited-map `Rc::as_ptr`), konversi dasar `angka` (Teks gagal → `[NaN]`, Bolean→1/0)/`teks`/`kamus` (objek atau Daftar pasangan `[k,v]`, malformed `[TYPE]`). Deviasi: `bolean`≡`ke_boolean`, `daftar`≡`ke_larik`. `impor utilitas` no-op. 50/50 test hijau (8 core + 42 vm), clippy 0, contoh `utilitas.eve` + regresi OK. **Lanjut: `impor` lintas berkas (v1 minimal).**
- **2026-09-10**: **Fase 3 item 5 selesai — Modul SISTEM** (10 builtin id 52–61). `baca_file/tulis_file/ada_file` (std::fs, `[FILE]`), `env(nama,bawaan?)` `[ENV]` jika hilang tanpa bawaan, `atur_env`, `waktu_sekarang`, `tanggal_sekarang(ts?)` & `format_tanggal(ts,pola)` (UTC ISO, token YYYY/MM/DD/HH/mm/ss — algoritma Hinnant zero-dep), `tunda` (negatif `[WAKTU]`), `selisih_waktu`. **Defer:** handle berkas `buka/tutup/baca_baris/tulis` + `argumen()` (→ Fase 4). `impor sistem` no-op. Kode error baru `[ENV]`/`[WAKTU]` ditambah di `ERROR.md`. 44/44 test hijau (8 core + 36 vm), clippy 0. Contoh `sistem.eve` OK. Lanjut: modul **utilitas**.
- **2026-09-10**: **Fase 3 item 4 selesai — Modul DAFTAR + KAMUS + statement `hapus`.** 16 fungsi daftar (id 28–43: tambah/sisip/hapus/urutkan/balik/unik/jumlah/rata_rata/gabung_larik/iris/cari/ada/peta/saring/lipat/setiap) & 8 fungsi kamus (id 44–51). Keyword `hapus` baru (statement): `hapus a[i]`, `hapus k["x"]`, `hapus k.nama` — indeks daftar wrap negatif/out-of-range `[INDEX]`, kunci hilang diam-diam. `hapus(...)` sebagai panggilan fungsi juga jalan (disambiguasi parser, id 30). **Higher-order `peta/saring/lipat/setiap`** via pending-callback stack di VM (nested callback OK, `lipat([])` → awal, arity error `[FUNGSI]`, error callback merambat ke `coba/tangkap`). Mutasi in-place vs daftar baru, `urutkan` homogen saja, `kunci/nilai/pasangan` diurut alfabetis, `dapatkan(k,k,bawaan?)` `[KEY]` tanpa bawaan. Clippy 0 warning, **40/40 test hijau**, contoh `daftar.eve` + `kamus.eve` + regresi lama OK. `STDLIB.md` §C/§E, `ARRAY.md`, `RENCANA.md`, `PROGRES.md` disinkron. Lanjut: modul **sistem** (menunggu arahan user).
- **2026-09-10**: **Fase 3 item 3 selesai — Modul MATEMATIKA** (16 fungsi + pi/e global via `PanggilBuiltin` id 12–27). User-fungsi mengalahkan builtin (`declared_fns`). Error `[MATH]`/`[JUMLAH]`. `vm_tests.rs` ditulis ulang jadi test Rust klasik (18 vm + 8 core = 26/26). Contoh `matematika.eve` OK. Lanjut: **daftar & kamus (+`hapus`)**.
- **2026-09-10**: **Plan Fase 4 +2 item** (keputusan user): **`evernight coba.eve` default-run** (tanpa flag langsung jalan — buang keharusan `--run` panjang) + **biner global/PATH** (`cargo install --path crates/evernight_cli`). Di-catat di `RENCANA.md` & `PROGRES.md`. **Fase 3 berhenti dulu — menunggu arahan user untuk modul selanjutnya.**
- **2026-09-10**: **Fase 3 item 2 selesai — Modul STRING.** 12 fungsi global via **1 opcode generik `PanggilBuiltin` (0x94)** dengan tabel dispatch (dipakai ulang semua stdlib). `impor string` no-op. 49/49 test (8 core + 41 vm); contoh `string.eve` OK. Lanjut: modul **matematika** (juga via `PanggilBuiltin`).
- **2026-09-10**: **Biner `evernight` + asosiasi file final.** Biner crusher/runner sekarang `evernight` (`evernight file.eve`, crate `evernight_cli`). Spek asosiasi Windows dikunci: `.eve` → **"Evernight files"** (ProgID `EvernightFile`, icon.ico, `evernight.exe "%1"`) — Addendum `RENCANA.md` Fase 6, dieksekusi installer Fase 6.
- **2026-09-10**: **Fase 3 item 1 selesai — Modul konsol GLOBAL builtin.** `baca(pesan?)`, `baca_angka(pesan?)` (non-angka → `BAHAYA [NaN]`), `bersihkan()`. Opcode baru `BacaAngka`/`Bersihkan`; `Vm.input_queue` utk test stdin; `impor konsol` no-op. 42/42 test; contoh `konsol.eve` OK. Lanjut: modul **string**.
- **2026-09-10**: **C dihapus permanen dari proyek** — keputusan final user. `c_interop/` (FFI) keluar dari semua dokumen; implementasi = **Rust murni** + **TypeScript** (ekstensi editor VS Code Fase 4). Scaffolding TS dibuat di `editors/vscode/`. Rust runtime tidak akan menyentuh FFI; optimasi hot path via Rust saja.
- **2026-09-10**: **Audit Fase 2 — hasil**: 9/11 item terkonfirmasi selesai; 2 temuan: `c_interop/` defer (sudah ditandai), dan keyword **`hapus` (del) belum ada di seluruh codebase**. Keputusan: `hapus` dipindah ke **Fase 3** (bersama modul koleksi daftar/kamus) — Opsi B. Sinkron `RENCANA.md`, `PROGRES.md`.
- **2026-09-10**: **RENCANA 5→7 FASE disetujui & dieksekusi.** Fase 3 & 4 diberi blok baru: Fase 3 = Standard Library **+ Identitas Visual** (`LOGO.md`: brief logo + icon `.eve` + brand kit, user yang desain); Fase 4 = REPL & CLI **+ Highlighting Editor** (`TEMA.md`: pemetaan token→scope, palet "Nusantara" terang/gelap, grammar TextMate digenerate dari `token.rs`, ekstensi VS Code). Fase 6/7 baru. Fase 3–7 masing-masing 12–15 item. File MD baru dibuat (5): `LOGO.md`, `TEMA.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`. Sinkron: `RENCANA.md`, `PROGRES.md`, `AGENTS.md`, `STRUKTUR.md`, `KONTEKS.md`. Struktur project ditambah dir `assets/logo/`, `editors/vscode/`, `paket/`, `situs/`. **Menunggu user**: arah desain logo (`LOGO.md` §3) + palet final (`TEMA.md` §3).
- **2026-09-10**: **Compiler di-refactor 4 sub-modul + VM error-handling + FITUR BACKEND LENGKAP.** `compiler/{mod,codegen,expressions,statements}.rs` menggantikan `compiler.rs`. Berfungsi: `untuk i dari A sampai B` & `untuk x dalam [daftar/teks]`, `cocok { kasus <expr> { } | kasus _ { } | bawaan { } }` (blok `{}`, wildcard kecuali yang disebut eksplisit `kasus _`), `coba { } tangkap (var) { }` (parentesis WAJIB setelah `tangkap`) + `lempar("msg")`, `pastikan (kondisi, pesan)` (parentesis wajib), penugasan gabungan, assign indeks (`k[i]=v`, pendukung kamus `k["x"]`) & properti. VM: sistem handler (`BuatHandler`/`PopHandler`, catch hanya chunk sama `Rc::ptr_eq`), kode error spesifik VARIABLE/DIVISION/TYPE/INDEX/KEY/FUNGSI/RUNTIME/ASSERT; compiler peringatan WKVAR/WKREACH. **36/36 test** (8+28). Perbaikan kunci: `LompatJikaSalah`/`LompatJikaBenar` MEM-POP kondisi (jangan emit `Pop` ekstra → menghapus slot lokal), for-loop init sebelum `start_ip` (loop-back tidak reset counter), `continue_ip` di LoopInfo, handler di-patch SETELAH `Lompat skip`, match tanpa Pop di lintasan belum-cocok (`SamaDengan` sudah makan duplikat), `SimpanIndeks`/`SimpanKunci` kembalikan nilai (ekspresi assign), `daftar.panjang`/`teks.panjang`/`kamus.panjang`. Sintaks range = `dari...sampai` (bukan `..=`)! Contoh `examples/fitur_baru.eve` baru.
- **2026-09-10**: Backend `evernight_vm` selesai & berfungsi: `value.rs` (+Value::Fungsi), `bytecode.rs`, `compiler.rs`, `vm.rs` (stack VM + call frames). `hello.eve` → `Halo Dunia dari EvernightLanguage!`, `faktorial.eve` → `Faktorial:120` (rekursi jalan). Build & 8/8 test hijau. Perbaikan penting: resolve local forward-index (bug reversed), global var pakai operand u16 benar, `cetak` tanpa dummy push, `Henti` return Kosong, if/elif/else & while+break/continue dirombak + scoping. Sisa celah: `untuk`/`cocok`/`coba`/`stlib` masih no-op, belum ada unit test vm.
- **2026-09-09**: Toolchain Rust final aktif: `stable-x86_64-pc-windows-gnu` 1.98.1. MSVC dibatalkan (butuh VS Build Tools `link.exe` + Windows SDK import libs). Jalur GNU: `rust-lld` bawaan + `+crt-static` (tanpa gcc). `cargo build --workspace` & `cargo test --workspace` HIJAU (3 test lexer lolos). `ast.rs` + `parser.rs` terkompilasi penuh. Blocker baru: AV McAfee/Reason mengkarantina `evernight_cli.exe` saat dijalankan.
- **2026-09-08**: Inisialisasi Cargo Workspace (`evernight_core`, `evernight_vm`, `evernight_cli`), implementasi `token.rs` (~50 keyword, literal, operator), `errors.rs` (format BAHAYA/PERINGATAN), `lexer.rs` (handwritten zero-deps), serta `evernight_cli/src/main.rs`.
- **2026-09-08**: Sinkronisasi arsitektur Fase 2 ke model Cargo Workspace (`evernight_core`, `evernight_vm`, `evernight_cli`), Handwritten Lexer & Pratt Parser, Zero-deps, Stack VM, dan Safe ARC Memory Model pada `STRUKTUR.md`, `RENCANA.md`, `PROGRES.md`.
- **2026-09-08**: `LIFECYCLE.md` difinalisasi — 8 tahap runtime (baca→lexer→parser→compiler→VM init→eksekusi→error→cleanup), hybrid REPL+File, lazy-load impor.
- **2026-09-08**: Wildcard `_` ditambahkan ke `cocok`/pattern matching di `KEYWORD.md`, `GRAMMAR.md`, `RENCANA.md`, `KONSEP.md`, `AGENTS.md`. `adalah` (is) & `lewati` (pass) diputuskan tidak perlu. `hapus` (del) tetap di Fase 2 cadangan.
- **2026-09-08**: `STRUKTUR.md` dibuat — peta struktur project Rust lengkap; `RENCANA.md` struktur section diupdate.
- **2026-09-08**: Finalisasi `KEYWORD.md` — semua kata kunci Fase 1 ditetapkan. Update `ARRAY.md`, `TIPE.md`, `STDLIB.md`, `FUNCTION.md`, `RENCANA.md` untuk sinkronisasi nama keyword baru.
- **2026-09-07**: `FUNCTION.md` dan penambahan masif pada `STDLIB.md` selesai difinalisasi.
- **2026-09-07**: `STDLIB.md` dibuat dan difinalkan.
- **2026-09-07**: `ERROR.md` dibuat dan difinalkan; `RENCANA.md`, `PROGRES.md`, serta `KONTEKS.md` disinkronkan.
- **2026-09-07**: `TIPE.md` dibuat dan difinalkan; `RENCANA.md`, `PROGRES.md`, serta `KONTEKS.md` disinkronkan.
- **2026-09-07**: File `KONTEKS.md` dibuat. Aturan baca-pertama ditambahkan ke `AGENTS.md`.
